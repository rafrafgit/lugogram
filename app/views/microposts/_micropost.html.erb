<li>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago to <%= micropost.recipients %>
  </span>
  <%= render 'shared/lugogram_message', avatar: current_user.avatar, name: current_user.name, message: micropost.content, color: micropost.filter %>
</li>

