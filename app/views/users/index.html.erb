<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag(user.getAvatarURL, :size => "48x48") %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>